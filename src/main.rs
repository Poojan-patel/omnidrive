// omnidrive — entry point.
//
// M1: project scaffold only. This is a placeholder so the Cargo binary
// compiles; real wiring starts in M2.

fn main() {
    println!("omnidrive scaffolded. Implementation begins in M2.");
}
